export default class User {
  constructor(username, email, password, firstname, surname) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.firstName = firstname;
    this.surname = surname;
  }
}
